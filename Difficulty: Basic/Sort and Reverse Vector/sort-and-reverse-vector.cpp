// Function to sort the given vector
vector<int> sortVector(vector<int> v) {
    // code here
    sort(v.begin(),v.end());
    return v;
    
}

// Function to reverse the order of elements
vector<int> reverseVector(vector<int> v) {
    reverse(v.begin(),v.end());
    
    return v;

    
    // code here
}